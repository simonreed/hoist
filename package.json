{
  "name": "hoist",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "hoist": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile --minify src/cli.ts --outfile dist/hoist"
  },
  "engines": {
    "bun": ">=1.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.11"
  }
}
