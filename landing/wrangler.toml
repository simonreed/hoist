name = "hoist-sh"
main = "worker.js"
compatibility_date = "2024-01-01"

[assets]
# No static asset serving — worker handles all routes

[[rules]]
type = "Text"
globs = ["**/*.sh", "**/*.html"]
fallthrough = false
